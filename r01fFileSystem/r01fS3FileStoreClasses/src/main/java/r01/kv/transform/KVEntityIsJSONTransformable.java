package r01.kv.transform;

/**
 * Just a marker interface to indicate that the CRUD must transform the model object to JSON.
 */
public interface KVEntityIsJSONTransformable {
	// just a marker interface
}
